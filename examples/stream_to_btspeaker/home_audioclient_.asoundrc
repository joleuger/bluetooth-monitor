# See /usr/share/doc/libasound2-plugins/README-pulse

pcm.snapcast {
  type pulse
  device snapcast
  
  hint {
    show on
    description "snapcast device (PulseAudio)"
  }
}

ctl.snapcast {
  type pulse
  device snapcast
}
