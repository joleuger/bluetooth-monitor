# See /usr/share/doc/libasound2-plugins/README-pulse

pcm.virtual {
  type pulse
  device virtual
  
  hint {
    show on
    description "virtual device (PulseAudio)"
  }
}

ctl.virtual {
  type pulse
  device virtual
}
